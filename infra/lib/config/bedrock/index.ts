/**
 * @format
 * Bedrock Project Configuration - Barrel Export
 *
 * Centralized configuration for Bedrock project infrastructure.
 * Import from this file to access all allocations and configurations.
 *
 * @example
 * ```typescript
 * import {
 *     getBedrockAllocations,
 *     getBedrockConfigs,
 * } from '../../config/bedrock';
 * ```
 */

// Export all allocations
export * from './allocations';

// Export all configurations
export * from './configurations';
