# @format
# GitHub Actions Exporter — CI/CD metrics for Prometheus
apiVersion: v1
kind: Secret
metadata:
  name: github-actions-exporter-credentials
  namespace: monitoring
  labels:
    app: github-actions-exporter
type: Opaque
stringData:
  github-token: "__GITHUB_TOKEN__"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-actions-exporter
  namespace: monitoring
  labels:
    app: github-actions-exporter
    app.kubernetes.io/part-of: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-actions-exporter
  template:
    metadata:
      labels:
        app: github-actions-exporter
    spec:
      containers:
        - name: github-actions-exporter
          image: ghcr.io/labbs/github-actions-exporter:latest
          ports:
            - name: metrics
              containerPort: 9101
              protocol: TCP
          env:
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-actions-exporter-credentials
                  key: github-token
            - name: GITHUB_REPOS
              value: "Nelson-Lamounier/cdk-monitoring"
            - name: GITHUB_REFRESH
              value: "30"
            - name: PORT
              value: "9101"
            - name: EXPORT_FIELDS
              value: "repo,id,node_id,head_branch,head_sha,run_number,workflow_id,workflow,event,status"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: github-actions-exporter
  namespace: monitoring
  labels:
    app: github-actions-exporter
spec:
  type: ClusterIP
  selector:
    app: github-actions-exporter
  ports:
    - name: metrics
      port: 9101
      targetPort: metrics
      protocol: TCP
