# @format
# Grafana admin credentials — templated for deploy-time injection
# The deploy script replaces GRAFANA_ADMIN_PASSWORD from SSM or env
apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: monitoring
  labels:
    app: grafana
type: Opaque
stringData:
  admin-user: admin
  admin-password: "__GRAFANA_ADMIN_PASSWORD__"
