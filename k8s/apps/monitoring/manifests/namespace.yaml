# @format
# Monitoring namespace — isolates all observability workloads
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  annotations:
    kubernetes.io/description: "Namespace for the monitoring stack (Prometheus, Grafana, Loki, Tempo)"
  labels:
    app.kubernetes.io/part-of: monitoring
    app.kubernetes.io/managed-by: kustomize
