{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: start-admin
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "start-admin.fullLabels" . | nindent 4 }}
spec:
  entryPoints:
    - web
  routes:
    - match: {{ .Values.ingress.matchRule }}
      kind: Rule
      services:
        - name: start-admin
          port: {{ .Values.service.port }}
{{- end }}
