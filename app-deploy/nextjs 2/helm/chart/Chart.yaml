# @format
# Next.js Helm Chart — Full Application
#
# Manages all Next.js Kubernetes resources including:
#   - Namespace, ConfigMap, Secrets (placeholders)
#   - Deployment with topology-aware scheduling
#   - Service, IngressRoute, HPA, NetworkPolicy, ResourceQuota
#   - Alloy sidecar configuration for OTLP tracing
#
# ArgoCD manages this chart as a Helm-type source.
# Secrets are resolved from SSM by deploy-manifests.sh and passed
# via helm upgrade --set or kubectl create secret.
#
# Usage:
#   helm upgrade --install nextjs-app ./chart -n nextjs-app -f ../nextjs-values.yaml --wait

apiVersion: v2
name: nextjs-app
description: Next.js portfolio application for Kubernetes
type: application
version: 0.2.0
appVersion: "1.0.0"
